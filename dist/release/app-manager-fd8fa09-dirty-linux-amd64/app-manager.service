[Unit]
Description=App Manager Service
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/app-manager
ExecStart=/opt/app-manager/app-manager server/config.sqlite.yaml
Restart=always
User=root

[Install]
WantedBy=multi-user.target
