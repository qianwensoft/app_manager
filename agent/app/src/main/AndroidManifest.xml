<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.appmanager.agent">

    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 蓝牙打印（经典蓝牙 SPP + BLE）：<=30 用旧权限，31+ 用运行时新权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- 前台 Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 通知（Android 13+ 运行时权限）：前台服务通知须能展示，否则 FGS 在 14+/16 上不可靠甚至被杀 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 开机自启 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- APK 安装 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 文件读写 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Android 11+ 完整文件访问（须在 Manifest 声明后系统设置才会出现授权项） -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Android 13+ 分类媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- 硬件信息 -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />

    <!-- 前台应用检测（需要在系统设置中手动授权） -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Logcat 读取（signature/privileged 权限，需通过 adb pm grant 授权） -->
    <uses-permission android:name="android.permission.READ_LOGS" />

    <!-- 悬浮窗 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 动态权限（DeviceInfo 页面） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="磐石 Agent"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppManagerMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.appmanager.agent.ACTION_REQUEST_SCREEN" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.appmanager.agent.ACTION_OPEN_SCADA_MENU" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.appmanager.agent.ACTION_OPEN_WIRELESS_ADB" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ScadaWebViewActivity"
            android:exported="false"
            android:label="@string/scada_webview_title"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.DeviceInfoActivity"
            android:exported="false"
            android:label="设备信息" />

        <activity
            android:name=".ui.BackendMenuActivity"
            android:exported="false"
            android:label="@string/main_tile_backend"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.PersonalCenterActivity"
            android:exported="false"
            android:label="@string/personal_center_title"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.CatalogListActivity"
            android:exported="false"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.DeviceListenStateActivity"
            android:exported="false"
            android:label="@string/main_tile_listen_state"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:label="@string/about_title"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".ui.BackendMenuActivity" />

        <activity
            android:name=".ui.SystemUpdateActivity"
            android:exported="false"
            android:label="@string/system_update_title"
            android:theme="@style/Theme.AppManagerMain"
            android:parentActivityName=".ui.BackendMenuActivity" />

        <activity
            android:name=".ui.InAppWebActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity
            android:name=".FormAppActivity"
            android:exported="false"
            android:label="表单应用"
            android:theme="@style/Theme.AppManagerMain"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <service
            android:name=".service.AgentService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaProjection" />

        <service
            android:name=".service.TouchAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".InstallStatusReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
