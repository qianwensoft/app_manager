# SCADA 模块基础图形 SVG 重构 + 钢笔/铅笔/多边形工具

## 概述

将 SCADA 编辑器的基础图形从 Canvas 2D 绘制改为独立 SVG 元素渲染，并新增钢笔工具、铅笔工具和多边形工具，支持自由路径绘制和任意多边形创建。

## 主要变更

### 1. 类型定义扩展

**文件**: `scada-editor/src/types/index.ts`

- 在 `CanvasElement` 接口中添加路径相关属性：
  - `pathData?: string` — SVG path d 属性
  - `pathPoints?: Array<...>` — 原始路径点数据（用于编辑）
  - `pathClosed?: boolean` — 路径是否闭合

- 在 `ElementType` 联合类型中添加：
  - `'path'` — 钢笔工具绘制的贝塞尔曲线路径
  - `'pencil'` — 铅笔工具绘制的自由曲线
  - `'polygon'` — 多边形工具绘制的任意多边形

- 在 `DrawingTool` 类型中添加：
  - `'path'` — 钢笔绘制模式
  - `'pencil'` — 铅笔绘制模式
  - `'polygon'` — 多边形绘制模式

### 2. SVG 渲染组件

**新文件**: `scada-editor/src/components/ShapeSvg.tsx`

独立的 SVG 形状渲染组件，支持：
- **基础图形**: rect, circle, ellipse, line, polyline, polygon
- **路径图形**: path（钢笔工具）, pencil（铅笔工具）, polygon（多边形工具）
- **条件样式**: 支持运行时根据数据绑定动态改变填充色和描边色
- **选中状态**: 显示蓝色虚线边框
- **变换**: 支持旋转、透明度等 CSS 变换

每个图形元素渲染为独立的 SVG DOM 元素，替代之前的 Canvas 2D 绘制。

### 3. 路径工具辅助函数

**新文件**: `scada-editor/src/utils/pathTools.ts`

提供路径处理工具函数：
- `pointsToPathData()` — 将点数组转换为 SVG path d 属性字符串
- `simplifyPath()` — Douglas-Peucker 算法简化路径点（用于铅笔工具平滑化）
- `smoothPath()` — Catmull-Rom 样条平滑路径（铅笔工具生成流畅曲线）
- `distance()` — 计算两点间距离
- `generateRegularPolygon()` — 生成正多边形顶点（三角形、正方形、五边形、六边形等）
- `generateStar()` — 生成星形顶点（五角星、六角星、八角星等）

### 4. 编辑器交互逻辑

**文件**: `scada-editor/src/components/CanvasBoard.tsx`

#### 钢笔工具 (path)
- **鼠标左键点击**: 添加路径点，逐点构建路径
- **双击或 Enter 键**: 完成路径绘制，创建元素
- **Esc 键**: 取消当前绘制
- **路径类型**: 直线连接（后续可扩展为贝塞尔曲线）

#### 多边形工具 (polygon)
- **鼠标左键点击**: 添加多边形顶点
- **点击起点附近**: 自动闭合多边形（起点显示绿色圆点）
- **双击或 Enter 键**: 完成多边形绘制，创建元素
- **Esc 键**: 取消当前绘制
- **实时预览**: 绘制过程中显示半透明填充和顶点标记
- **快捷创建**: 属性面板提供正多边形（三角形、正方形、五边形、六边形、八边形）和星形（五角星、六角星、八角星）快捷按钮

#### 铅笔工具 (pencil)
- **按住鼠标拖动**: 记录所有鼠标移动轨迹点
- **松开鼠标**: 自动简化并平滑路径，创建元素
- **算法**: Douglas-Peucker 简化 + Catmull-Rom 样条平滑

#### Canvas 绘制优化
- 只在 Canvas 上绘制 `dynamic-pipe`（动态管道）
- 其他基础图形全部改用 SVG DOM 元素渲染
- 保留网格和选择框的 Canvas 绘制

### 5. 预览/运行时支持

**文件**: `scada-editor/src/components/CanvasViewer.tsx`

运行时同样使用 SVG 渲染基础图形：
- 提取 `shapeElements` 过滤器
- 映射到 `ShapeSvg` 组件
- 支持条件样式动态绑定
- 保持与编辑器一致的渲染逻辑

### 6. 组件面板

**文件**: `scada-editor/src/components/WidgetPanel.tsx`

在"基础图形"分组中添加：
- **钢笔工具**: 图标为钢笔路径，默认蓝色描边
- **铅笔工具**: 图标为铅笔，默认蓝色描边
- **多边形工具**: 图标为多边形，默认半透明蓝色填充 + 蓝色描边

拖拽到画布即可开始绘制。

### 7. 属性面板

**文件**: `scada-editor/src/components/PropertiesPanel.tsx`

为路径元素（`path`, `pencil`, `polygon`）添加专属面板：
- **闭合路径**: 切换开关，控制路径是否闭合
- **路径点数**: 显示当前路径包含的点数量
- **快捷创建正多边形**（仅 polygon）:
  - 三角形 (△)、正方形 (◇)、五边形 (⬟)、六边形 (⬡)、八边形
  - 一键生成规则多边形，自动居中于当前元素边界
- **快捷创建星形**（仅 polygon）:
  - 五角星 (★5)、六角星 (★6)、八角星 (★8)
  - 一键生成星形图案
- **操作提示**: 
  - 钢笔工具: "双击画布完成钢笔路径绘制"
  - 铅笔工具: "松开鼠标自动平滑铅笔路径"
  - 多边形工具: "点击添加顶点，双击或点击起点完成多边形"

### 8. 服务器缓存控制

**文件**: `server/api/router.go`

为 scada-editor 静态文件添加缓存控制头：
```go
scadaEditorGroup := r.Group("/scada-editor")
scadaEditorGroup.Use(func(c *gin.Context) {
    c.Header("Cache-Control", "no-cache, no-store, must-revalidate")
    c.Header("Pragma", "no-cache")
    c.Header("Expires", "0")
    c.Next()
})
scadaEditorGroup.StaticFS("", gin.Dir(scadaEditorDir, false))
```

防止浏览器缓存导致加载旧版本 JavaScript 文件。

## 优势

### 1. 性能提升
- **独立 DOM 元素**: 每个图形是独立的 DOM 节点，浏览器可以独立优化
- **GPU 加速**: SVG 变换（旋转、缩放）可利用硬件加速
- **局部更新**: 修改单个图形不需要重绘整个画布

### 2. 功能增强
- **事件监听**: SVG 元素可以直接绑定鼠标事件（Canvas 需要手动 hit test）
- **CSS 样式**: 可以使用 CSS 动画、过渡效果
- **选择器**: 可以使用 CSS 选择器直接操作图形

### 3. 代码可维护性
- **关注点分离**: 每个图形类型有独立的渲染逻辑
- **组件化**: SVG 图形可以作为 React 组件复用
- **调试友好**: DOM 树可见，方便调试和检查

### 4. 扩展性
- **自定义路径**: 钢笔工具支持任意复杂路径
- **自由绘制**: 铅笔工具支持手绘风格线条
- **易于扩展**: 未来可添加更多路径编辑功能（编辑控制点、曲线调整等）

## 使用说明

### 基础图形
1. 从左侧组件面板拖拽基础图形（矩形、圆形、椭圆、直线）到画布
2. 图形会以 SVG 元素形式渲染
3. 在属性面板中设置填充色、描边色、描边宽度等

### 钢笔工具
1. 选择钢笔工具（或拖拽钢笔组件到画布）
2. 在画布上点击添加路径点
3. 双击完成路径绘制
4. 在属性面板中可以切换"闭合路径"

### 铅笔工具
1. 选择铅笔工具（或拖拽铅笔组件到画布）
2. 按住鼠标拖动绘制自由曲线
3. 松开鼠标自动完成并平滑路径
4. 系统会自动简化路径点，生成流畅曲线

### 多边形工具
1. 选择多边形工具（或拖拽多边形组件到画布）
2. 在画布上点击添加顶点（至少3个）
3. 点击起点附近或双击或按 Enter 键完成多边形
4. 按 Esc 键取消绘制
5. 在属性面板中可以：
   - 一键生成正多边形（三角形、正方形、五边形等）
   - 一键生成星形（五角星、六角星等）
   - 切换路径开放/闭合状态

### 路径编辑
- 创建路径后，可以在属性面板中：
  - 切换路径开放/闭合状态
  - 查看路径点数量
  - 调整描边颜色和宽度
  - 设置填充色（闭合路径生效）

## 技术细节

### SVG 坐标系统
- SVG 元素使用绝对定位 + 内部相对坐标
- 变换（旋转）通过 CSS transform 实现
- 缩放通过 SVG transform 属性实现

### 路径简化算法
铅笔工具使用 Douglas-Peucker 算法：
- 保留关键转折点
- 去除冗余中间点
- tolerance = 3px（可调整）

### 路径平滑算法
铅笔工具使用 Catmull-Rom 样条：
- 生成 C（三次贝塞尔）曲线
- tension = 0.5（可调整）
- 曲线通过所有简化后的控制点

## 兼容性

### 浏览器兼容性
- Chrome/Edge: 完全支持
- Firefox: 完全支持
- Safari: 完全支持
- IE11: 不支持（已废弃）

### 数据迁移
现有的 Canvas 绘制图形会在加载时自动转换为 SVG 渲染，无需手动迁移数据。

## 构建与部署

```bash
# 构建前端
cd scada-editor && npm run build

# 复制到服务器静态目录
cd .. && rm -rf web/dist/scada-editor && cp -r scada-editor/dist web/dist/scada-editor

# 重新构建服务器（应用缓存控制改动）
make server-only

# 重启服务器
# ... 根据你的部署方式重启
```

## 后续优化方向

### 1. 钢笔工具增强
- [ ] 支持贝塞尔曲线控制点编辑
- [ ] 添加平滑/锐角切换
- [ ] 支持路径点拖动调整
- [ ] 支持删除/插入路径点

### 2. 铅笔工具增强
- [ ] 可调整平滑度参数
- [ ] 支持压力感应（触摸屏/数位板）
- [ ] 提供多种线条风格（手绘、书法等）

### 3. 多边形工具增强
- [x] ~~快捷创建正多边形（三角形、正方形、五边形等）~~
- [x] ~~星形工具~~
- [ ] 支持拖动顶点编辑多边形
- [ ] 添加圆角多边形选项
- [ ] 支持凹多边形创建

### 4. 性能优化
- [ ] 大量图形时使用虚拟化渲染
- [ ] 路径点过多时自动降采样
- [ ] 复杂路径使用 Canvas fallback

### 4. 功能扩展
- [x] ~~多边形工具~~
- [x] ~~星形工具~~
- [ ] 文字路径（文本沿路径排列）
- [ ] 路径动画（描边动画、路径变形）
- [ ] 路径布尔运算（合并、差集、交集）

## 测试清单

- [x] 矩形、圆形、椭圆在编辑器正确渲染
- [x] 钢笔工具���击添加点、双击完成
- [x] 铅笔工具拖动绘制、自动平滑
- [x] 路径闭合/开放切换生效
- [x] 属性面板填充色/描边色修改生效
- [x] 预览模式下图形正确显示
- [x] 条件样式在运行时动态变化
- [x] 缩放和旋转变换正常
- [x] 导出的 JSON 包含路径数据
- [x] 浏览器强制刷新加载新代码

## 版本信息

- **重构日期**: 2026-08-29
- **影响模块**: scada-editor (前端), server/api (后端)
- **Git Commit**: 待提交
- **Breaking Changes**: 无（向后兼容）
